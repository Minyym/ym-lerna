{
  "name": "ym-lerna",
  "version": "1.1.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "lerna version --conventional-commits --yes && lerna publish from-package --access public",
    "release:patch": "lerna publish patch --yes --access public",
    "release:minor": "lerna publish minor --yes --access public",
    "release:major": "lerna publish major --yes --access public",
    "prerelease": "lerna publish prerelease --preid beta --yes --access public",
    "preminor:beta": "lerna publish preminor --preid beta --yes --access public",
    "premajor:beta": "lerna publish premajor --preid beta --yes --access public",
    "prepatch:beta": "lerna publish prepatch --preid beta --yes --access public"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.2.3"
  }
}